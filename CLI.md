# PicSizer 命令行 (CLI) 使用手册

PicSizer 支持在 Windows 控制台 (CMD / PowerShell) 下通过命令行参数运行. 通过 CLI 模式, 可以将图片压缩功能无缝集成到自动化脚本. 

> 💡 **参数语法说明:**
> 1. 在 Go 标准库 `flag` 的实现中, 完整指令 (如 `--input`) 和简短指令 (如 `-i`) 通过别名绑定, 其效果完全等价. 
> 2. 布尔值参数 (`bool`) 如果为 `true`, 可以直接写参数名, 例如 `-c` 等同于 `-c=true`. 
>
>

---

## 一、 参数总览

### 1. 运行模式与全局控制

| 完整指令 | 简短指令 | 参数类型 | 默认值     | 说明 |
| --- | --- | --- |---------| --- |
| `--cli` | `-c` | bool | `false` | 启用命令行模式. 若不指定, 程序将默认启动图形界面.  |
| `--threads` | `-t` | int | `2`     | 最大并发线程数. 批量压缩时开启的多线程数量.  |
| `--progress`| `-p` | bool | `true` | 是否在控制台显示进度条 |

### 2. 压缩模式设置

| 完整指令 | 简短指令 | 参数类型   | 默认值       | 说明                                      |
| --- | --- |--------|-----------|-----------------------------------------|
| `--comp-type` | `-ct` | string | `quality` | 压缩模式. 可选: `quality` (按质量压缩) 或 `size` (按大小压缩).  |
| `--quality` | `-q` | int    | `80`      | 质量值. 范围 `1-100`. 仅在 `-ct quality` 时生效.     |
| `--limit` | `-l` | string | `200kb`   | 限制文件大小. 仅在 `-ct size` 时生效. 直接写单位, 如 `200kb`、`2mb`, 大小写均可.  |
| `--exceed` | `-e` | bool   | `false`   | 是否接受超出限制. 为 `true` 时, 若二分查找后依然超限, 仍执行输出.    |

### 3. 输入与输出

| 完整指令 | 简短指令 | 参数类型 | 默认值 | 说明                                                                                   |
| --- | --- | --- | --- |--------------------------------------------------------------------------------------|
| `--input` | `-i` | string | `""`    | 输入路径. 可以是一个具体的图片文件路径, 或是一个包含图片的文件夹路径.  |
| `--output` | `-o` | string | `""`    | 输出路径. 配合输出方式 (`--out-type`) 使用.  |
| `--out-type` | `-ot` | string | `dir` | 输出方式. 可选: <br><br>• `dir`: 输出到统一目录<br><br>• `cover`: 直接覆盖源文件<br><br>• `struct`: 保留原有的目录树结构 |
| `--format` | `-f` | string | `jpeg` | 输出图像格式. 可选: `jpeg/jpg`、`png`、`webp` 或 `origin` (保持原格式).                                  |
| `--template` | `-tpl` | string | `"{id}"` | 文件名模板 (不含后缀). 支持变量: <br><br>• `{id}`: 递增序号<br><br>• `{name}`: 原文件名 |
| `--start-idx` | `-si` | int | `1` | 命名模板中 `{id}` 的起始计数器数值.  |

### 4. 图像预处理

| 完整指令 | 简短指令 | 参数类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `--alpha` | `-a` | string | `keep` | 透明通道处理. 可选: <br><br>• `keep`: 保留透明通道<br><br>• `smart`: 智能移除 (无透明像素则全转白底并移除通道) <br><br>• `remove`: 全部强制移除并融合白底 |
| `--scale` | `-s` | string | `none` | 图像缩放模式. 可选: <br><br>• `none`: 不操作<br><br>• `stretch`: 强制拉伸 (无视比例) <br><br>• `cover`: 等比外接<br><br>• `contain`: 等比内接 (大图缩小, 小图不放大) <br><br>• `crop`: 等比外接并居中裁剪到目标尺寸<br><br>• `lock`: 等比锁定单边 (宽或高必须有一项为 0)  |
| `--width` | `-w` | int | `0` | 缩放目标宽度 (像素). `0` 表示不限制.  |
| `--height` | `-hg` | int | `0` | 缩放目标高度 (像素). `0` 表示不限制.  |

### 5. 高级压缩设置 (Advanced) 

| 完整指令 | 简短指令 | 参数类型 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `--jpeg-quality` | `-jq` | int | `0` | JPEG 精细化画质 (1-100). `0` 表示按全局画质等级处理, 非 0 时覆盖 JPEG 格式的画质值. |
| `--webp-quality` | `-wq` | int | `0` | WebP 精细化画质 (1-100). `0` 表示按全局画质等级处理, 非 0 时覆盖 WebP 格式的画质值. |
| `--png-palette` | `-pp` | string | `mediancut` | PNG 调色盘生成算法. 可选: <br><br>• `mediancut`: 中位切分 (Median Cut), 速度快<br><br>• `kmeans`: 均值聚类 (K-Means), 对高反差颜色更敏感 |
| `--png-keep-alpha` | `-pka` | bool | `true` | PNG 索引格式压缩时是否预留透明像素. 为 `false` 时透明区域可能被填充. |
| `--png-dither` | `-pd` | bool | `true` | PNG 有损量化时是否启用 Floyd-Steinberg 抖动算法. 开启可平滑渐变但可能增加体积. |

---

## 二、 典型场景用法示例 (使用简短指令) 

### 1. 基础单张图片压缩

将单张 JPEG 图片按"清晰"画质进行压缩, 并输出到指定文件夹: 

```bash
PicSizer.exe -c -i "C:\Photos\demo.jpg" -o "D:\Output" -ot dir -ct quality -q clear

```

### 2. 批量二分查找目标大小 + 保留目录结构

递归遍历 `C:\raw_images` 目录, 将所有图片压缩到不超过 `500 KB` (采用二分法查找最优质量), 转换为 `WebP` 格式, 并**完全保留原本的子目录层级结构**导出到 `D:\target`: 

```bash
PicSizer.exe -c -i "C:\raw_images" -o "D:\target" -ot struct -ct size -l 500 -u kb -f webp

```

### 3. 强制缩放 + 智能移除透明度 + 覆盖原图

使用 8 线程高并发处理, 读取一张 PNG 图片, 智能检查若无透明像素则抽离通道, 强制拉伸调整尺寸为 `800x600`, 按质量 `85` 压缩并**直接替换覆盖源文件**: 

```bash
PicSizer.exe -c -i "D:\avatar.png" -ot cover -ct quality -q 85 -t 8 -a smart -s stretch -w 800 -hg 600

```

### 4. 等比锁定高度 + 批量重命名排序

将文件夹内的图片等比缩放至高度为 `1080` 像素 (宽度会自动按比例计算锁定, 输入 `0`), 转为 `JPEG` 统一导出, 文件名重命名为 `wallpaper_001.jpg`、`wallpaper_002.jpg` 递增: 

```bash
PicSizer.exe -c -i "C:\Wallpapers" -o "C:\Dist" -ot dir -f jpeg -s lock -w 0 -hg 1080 -tpl "wallpaper_{id}" -si 1

```

---

## 三、 注意事项

1. **覆盖警告:** 当使用 `-ot cover` (覆盖源文件) 时, 请务必提前备份重要图片, 此操作不可逆. 
2. **锁单边校验:** 当 `-s lock` (等比锁定单边) 时, `-w` 和 `-hg` 必须满足**有且仅有一项为 0** (例如 `-w 1280 -hg 0`)
3. **文件名重名保护:** 当输出方式不为覆盖源文件 (`-ot dir` 或 `-ot struct`) 时, 请确保 `-tpl` 命名模板中包含 `{id}` 或 `{name}` 变量, 否则多张图片可能会因为生成相同的名字而被后续文件覆盖. 